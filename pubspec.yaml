name: inkubox_app
description: Inkubox application.

publish_to: 'none'

version: 1.1.2+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^1.0.6
  cupertino_icons: ^1.0.2
  firebase_auth: ^1.1.2
  firebase_core: ^1.0.4
  get: ^4.1.4
  google_fonts: ^2.0.0
  image_picker: ^0.7.5
  package_info_plus: ^1.0.1
  responsive_builder: ^0.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/img/logo.png
